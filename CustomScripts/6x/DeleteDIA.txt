{
"commands": [
"virtual-switch ethernet delete vs  vs-${CIRCUIT_NAME} \n",
"virtual-circuit ethernet delete vc vc-${CIRCUIT_NAME} \n",
"traffic-profiling standard-profile delete port ${UNI_PORT} profile BW_PROF_${UNI_PORT}\n", 
"port unset port ${UNI_PORT} untagged-data-vs untagged-ctrl-vs\n",
"vlan remove vlan ${SVLAN} port ${NNI_PORT}\n",

"vlan delete vlan ${SVLAN}\n",

	    	"configuration save\n"
]
}